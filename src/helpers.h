#pragma once

#include "maze.h"

namespace Helpers
{
    std::string parent_path();
    std::string shader_location(const std::string& file_name);
    std::string image_location(const std::string& file_name);
    
} // namespace Helpers
